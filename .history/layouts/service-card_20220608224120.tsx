import React, { FunctionComponent } from 'react'
import { IService } from '../type'

const ServiceCard:FunctionComponent<{service:IService}> = ({s}) => {
  return (
    <div>service-card</div>
  )
}

export default service-card